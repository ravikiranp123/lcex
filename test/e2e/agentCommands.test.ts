import * as assert from "node:assert";
import * as vscode from "vscode";
import { restoreAllStubs } from "./testUtils";

describe("Agent commands (4d.3)", () => {
  afterEach(() => restoreAllStubs());

  it.skip("leetplus.agentHint — opens copilot chat panel (cannot stub)", async () => {});

  it.skip("leetplus.agentAnalyze — opens copilot chat panel (cannot stub)", async () => {});

  it.skip("leetplus.agentMakeRunnable — opens copilot chat panel (cannot stub)", async () => {});
});
